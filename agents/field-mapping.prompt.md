# Field Mapping Agent

Use OpenClaw or a similar agent runner for:

- AI suggestions when a previously unseen ATS field is discovered

Inputs:

- raw field label
- job context
- existing profile field keys

Output:

- suggested canonical profile key
- confidence explanation

The final value should still be user-confirmed before automation resumes.

